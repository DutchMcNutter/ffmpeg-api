{
  "name": "ffmpeg-lambda",
  "version": "1.0.0",
  "description": "FFmpeg Lambda for video captioning with Whisper",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests configured\""
  },
  "dependencies": {
    "axios": "^1.6.0",
    "form-data": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
