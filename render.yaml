services:
  - type: web
    name: ffmpeg-api
    env: node
    buildCommand: apt-get update && apt-get install -y ffmpeg && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
